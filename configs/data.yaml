# YOLO Dataset Configuration for Manipal-UAV Person Detection Dataset
# Dataset paths (relative to project root)

# Image directories (absolute paths from project root)
# Note: Train images are directly in images/, validation/test are in images/images/
train: D:/SAR using rgb dataset/datasets/Manipal-UAV Person Detection Dataset/train/images
val: D:/SAR using rgb dataset/datasets/Manipal-UAV Person Detection Dataset/validation/images/images
test: D:/SAR using rgb dataset/datasets/Manipal-UAV Person Detection Dataset/test/images/images  # optional

# Number of classes
nc: 1

# Class names
names:
  0: person

# Additional metadata
dataset_type: rgb
image_size: 640
augment: true

